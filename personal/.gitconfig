# Git Configuration - Personal
# Personal Git settings that extend shared base configuration

# Include shared base configuration
[include]
	path = ./.gitconfig-base

# Personal user settings (overrides base)
[user]
	email = dev@joe.sh
	name = Joe Schmitt
