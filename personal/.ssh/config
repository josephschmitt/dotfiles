# SSH Configuration - Personal
# Personal SSH settings that extend shared base configuration

# Include shared base configuration FIRST so specific Host entries take precedence
Include ~/.ssh/config-base

# Added by OrbStack: 'orb' SSH host for Linux machines
# This only works if it's at the top of ssh_config (before any Host blocks).
# This won't be added again if you remove it.
Include ~/.orbstack/ssh/config

Host github.com
  IgnoreUnknown AddKeysToAgent,UseKeychain
  User git
  # Add your personal GitHub key here if different from work
