return {
  "folke/snacks.nvim",
  opts = {
    explorer = {
      enabled = true,
      hidden = true, -- Show hidden files by default
    },
    picker = {
      enabled = true,
      hidden = true, -- Show hidden files by default
    },
  },
}
