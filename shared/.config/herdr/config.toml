onboarding = false
new_cwd = "~/development"

[keys]
prefix = "ctrl+s"

# Pane navigation — Alt+hjkl (matches tmux-tilish / vim-tmux-navigator)
focus_pane_left = "alt+h"
focus_pane_down = "alt+j"
focus_pane_up = "alt+k"
focus_pane_right = "alt+l"

# Pane cycling — Alt+`/~ (matches tmux)
cycle_pane_next = "alt+backtick"
cycle_pane_previous = "alt+shift+backtick"

# Tab switching — Alt+1-9 (matches tmux-tilish window switching)
switch_tab = "alt+1..9"

# Tab navigation — prefix+[/]
previous_tab = "prefix+["
next_tab = "prefix+]"

# New tab — Alt+n (matches tmux new-window)
new_tab = "prefix+n"

# Agent navigation — Alt+[/]
previous_agent = "alt+["
next_agent = "alt+]"
focus_agent = "prefix+1..9"

# Workspace navigation (picker disabled — default conflicts with new_worktree)
workspace_picker = ""
new_workspace = "prefix+shift+n"
previous_workspace = "alt+shift+["
next_workspace = "alt+shift+]"
switch_workspace = "alt+shift+1..9"

# Split panes (matches tmux)
split_horizontal = "prefix+\\"
split_vertical = "prefix+|"

# Zoom — Alt+z
zoom = "alt+z"

# New worktree — moved from default prefix+G to free up prefix+G for lazygit
new_worktree = "prefix+w"

# Quick shell (closes on exit)
[[keys.command]]
key = "prefix+C"
type = "pane"
command = "$SHELL"

# Lazygit
[[keys.command]]
key = "prefix+G"
type = "pane"
command = "lazygit"

[ui]
show_agent_labels_on_pane_borders = true
agent_panel_scope = "all"

[ui.toast]
delivery = "terminal"
