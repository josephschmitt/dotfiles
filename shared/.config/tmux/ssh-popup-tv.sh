#!/bin/sh
# SSH host selector using television
# Smart SSH host detection with icons and connection info

tv ssh \
  --input-header "SSH Connect" \
  --input-prompt "  " \
  --no-remote \
  --hide-help-panel
