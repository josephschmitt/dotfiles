# Applications
# App split windows, application popups, and television popups

# Application split windows
bind-key A split-window -h -l 100 -c "#{pane_current_path}" "claude"
bind-key O split-window -h -l 100 -c "#{pane_current_path}" "opencode"
bind-key / split-window -h -l 100 -c "#{pane_current_path}" "scooter"

# Application windows
bind-key Y new-window -n "yazi" -c "#{pane_current_path}" "yazi"

# Application popups (using standardized tmux-popup script)
bind-key c run-shell "~/.config/tmux/scripts/tmux-popup -s xsmall -t 'Quick Shell' -E ~/.config/tmux/scripts/quick-shell.sh"
bind-key C run-shell "~/.config/tmux/scripts/tmux-popup -s xsmall -t 'Mini Shell' -E ~/.config/tmux/scripts/quick-shell.sh --persist"

bind-key E run-shell "~/.config/tmux/scripts/tmux-popup -w 100 -h 150 -t 'Editor' -E $EDITOR"
bind-key g run-shell "~/.config/tmux/scripts/lazygit-tab.sh"
bind-key -n M-g run-shell "~/.config/tmux/scripts/lazygit-tab.sh"
bind-key G run-shell "~/.config/tmux/scripts/tmux-popup -s medium -t 'Lazygit' -E lazygit || true"
bind-key m run-shell "~/.config/tmux/scripts/monocle-tab.sh"
bind-key -n M-m run-shell "~/.config/tmux/scripts/monocle-tab.sh"
bind-key M run-shell "~/.config/tmux/scripts/tmux-popup -s medium -t 'Monocle' -E monocle || true"
# bind-key C run-shell "~/.config/tmux/scripts/tmux-popup -s medium -t 'Scooter' -E scooter"

# Workmux key table (prefix + w enters workmux mode)
bind-key w switch-client -T workmux
bind-key -T workmux a run-shell "~/.config/tmux/scripts/tmux-popup -w 80 -h 10 -t ' Add Worktree' -E ~/.config/tmux/scripts/workmux-add.sh"
bind-key -T workmux r run-shell "~/.config/tmux/scripts/tmux-popup -w 80 -h 10 -t ' Remove Worktree' -E ~/.config/tmux/scripts/workmux-remove.sh"
bind-key -T workmux R run-shell "~/.config/tmux/scripts/tmux-popup -w 80 -h 10 -t ' Force Remove Worktree' -E '~/.config/tmux/scripts/workmux-remove.sh --force'"
bind-key -T workmux m run-shell "~/.config/tmux/scripts/tmux-popup -w 80 -h 20 -t ' Merge Worktree' -E ~/.config/tmux/scripts/workmux-merge.sh"
bind-key -T workmux d run-shell "~/.config/tmux/scripts/tmux-popup -s large -t 'Workmux' -E 'workmux dashboard'"
bind-key -T workmux t run-shell "~/.config/tmux/scripts/workmux-tab.sh"

# Television popups
bind-key f run-shell "~/.config/tmux/scripts/tmux-popup -d \$HOME -s large -E tv files || true"
bind-key P run-shell "~/.config/tmux/scripts/tmux-popup -d \$HOME -s large -E tv pj || true"
bind-key T run-shell "~/.config/tmux/scripts/tmux-popup -s large -E 'tv --show-remote' || true"
