-- Shared configuration constants used across multiple plugin files
return {
  -- Neo-tree sidebar width (used in filetree.lua, mini.lua for statusline offset)
  neotree_width = 40,

  -- Screen width threshold for auto-closing neo-tree (used in filetree.lua)
  neotree_auto_close_width = 150,
}
